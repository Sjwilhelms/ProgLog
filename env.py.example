### 3. Create an env.py.example File

Create a template for environment variables:

```python
import os

# Environment settings
os.environ.setdefault("DJANGO_ENVIRONMENT", "development")

# Django core settings
os.environ.setdefault("SECRET_KEY", "generate-a-new-secure-key-here")

# Database settings - only needed if using a custom database
os.environ.setdefault("DB_ENGINE", "django.db.backends.postgresql")
os.environ.setdefault("DB_NAME", "your_db_name")
os.environ.setdefault("DB_USER", "your_db_user")
os.environ.setdefault("DB_PASSWORD", "your_db_password")
os.environ.setdefault("DB_HOST", "localhost")
os.environ.setdefault("DB_PORT", "5432")

# Email settings
os.environ.setdefault("EMAIL_HOST", "smtp.example.com")
os.environ.setdefault("EMAIL_PORT", "587")
os.environ.setdefault("EMAIL_HOST_USER", "your_email@example.com")
os.environ.setdefault("EMAIL_HOST_PASSWORD", "your_email_password")

# Social authentication (if enabled)
# os.environ.setdefault("GOOGLE_CLIENT_ID", "your-client-id")
# os.environ.setdefault("GOOGLE_SECRET", "your-client-secret")